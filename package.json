{
  "name": "autonomous-job-apply-agent",
  "version": "1.0.0",
  "description": "AI-driven agent that discovers and applies to remote/hybrid jobs across ATSs (Greenhouse/Lever/Ashby/Workable/SmartRecruiters) — quality answers, EEO, captcha-aware, dedup.",
  "private": true,
  "scripts": {
    "setup": "npx playwright install chrome",
    "discover": "node src/discover-api.js",
    "apply": "node src/index.js"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "@playwright/test": "^1.50.0"
  }
}
